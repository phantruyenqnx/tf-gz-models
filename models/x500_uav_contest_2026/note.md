# Use to dettach joint (drop ball) by using cmd below:
gz topic -t /ball_1/detach -m gz.msgs.Empty -p ''
gz topic -t /ball_2/detach -m gz.msgs.Empty -p ''
gz topic -t /ball_3/detach -m gz.msgs.Empty -p ''
gz topic -t /ball_4/detach -m gz.msgs.Empty -p ''
gz topic -t /ball_5/detach -m gz.msgs.Empty -p ''